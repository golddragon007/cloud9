git config --global user.name $USER;git config --global user.email $EMAIL
git clone https://$GITHUB_USER:$GITHUB_PASS@github.com/ec-europa/$REPO.git

cd $REPO
git checkout -b toolkit/upgrade
set +e;rm *;mkdir RESOURCES;mv resources/patches RESOURCES/;mv resources/site.make RESOURCES/;mv resources/composer.json RESOURCES/;rm -r docs/ resources/ src/ tests/;set -e

echo running composer create-project ec-europa/subsite temp dev-master
composer create-project ec-europa/subsite temp dev-master
